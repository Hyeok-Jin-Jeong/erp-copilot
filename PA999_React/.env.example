# PA999 React 데모 UI — 환경변수
# 이 파일을 복사하여 .env 로 저장 후 실제 값 입력

# PA999S1 서버 주소 (기본값: http://localhost:5000)
VITE_API_BASE_URL=http://localhost:5000

# 데모 모드: true 이면 실제 API 대신 Mock 응답 사용 (서버 없이 UI 확인용)
VITE_DEMO_MODE=false
